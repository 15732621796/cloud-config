server:
  port: 8201
spring:
  profiles: dev
  application:
    name: jarvis-config-client

eureka:
  client:
    service-url:
      #客户端注册进eureka服务列表内
      defaultZone: http://eureka-dev.com:7001/eureka/

---

server:
  port: 8202
spring:
  profiles: test
  application:
    name: jarvis-config-client

eureka:
  client:
    service-url:
      #客户端注册进eureka服务列表内
      defaultZone: http://eureka-test.com:7001/eureka/
